# www.proveedorregional.cl — Caddy saca el certificado.
# Culpeo (micrófono) necesita HTTPS.

www.proveedorregional.cl {
	encode gzip
	reverse_proxy 127.0.0.1:3000
}

proveedorregional.cl {
	redir https://www.proveedorregional.cl{uri} permanent
}
